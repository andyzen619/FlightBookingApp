package cs.b07.cscb07project.backEnd.application;

/**
 * Constants used in the Flight Booking Application.
 * @author Raphael Alviz, Zain Amir, Ian Ferguson, Andy Liang, Johnathan Tsoi
 */
public class Constants {
  
  /** The minimum allowable time for a stop over. */
  public static final long STOPOVERMIN = 30;
  
  /** The maximum allowable time for a stop over. */
  public static final long STOPOVERMAX = 6;
  
  /** The format in which departure dates will be given. */
  public static final String DAY_DATE_FORMAT = new String("yyyy-MM-dd");
  
  /** The format in which flight arrival and departure times will be given. */
  public static final String TIME_DATE_FORMAT = new String("yyyy-MM-dd HH:mm");
  
  /** The format in which travel times will be given. */
  public static final String TIME_FORMAT = new String("HH:mm");

  /** Flight ListView row text. */
  public static final String FLIGHT_LISTVIEW_ROW = new String("%s     Flight Number: %s\n"
          + "Departing: %s @ %s\nArriving: %s @ %s\nSeats: %s     Total Price: $%s");

  /** Flight for Itinerary ListView row text. */
  public static final String FLIGHT_ITINERARY_LISTVIEW_ROW = new String("%s     Flight Number: %s\n"
          + "Departing: %s @ %s\nArriving: %s @ %s\nSeats: %s\n");

  /** Total cost and time for Itinerary ListView row text. */
  public static final String TOTAL_LISTVIEW_ROW = new String("Total Time: %s\nTotal Cost: %s");

  /** Key for username of current user. */
  public static final String USERNAME = new String("username");

  /** Key for client username. */
  public static final String CLIENT = new String("client");

  /** Key for departure date. */
  public static final String DEPARTURE_DATE = new String("departureDate");

  /** Key for origin. */
  public static final String ORIGIN = new String("origin");

  /** Key for destination. */
  public static final String DESTINATION = new String("destination");

  /** Key for flight number. */
  public static final String FLIGHT = new String("flight");

  /** Key for an itinerary. */
  public static final String ITINERARY = new String("itinerary");
}